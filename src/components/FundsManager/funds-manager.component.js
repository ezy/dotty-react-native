import * as React from 'react';
import {View, Text} from 'react-native';

const FundsManager = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>FundsManager</Text>
    </View>
  );
};

export default FundsManager;
