import * as React from 'react';
import {View, Text} from 'react-native';

const UserHeader = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>User Header Placeholder</Text>
    </View>
  );
};

export default UserHeader;
